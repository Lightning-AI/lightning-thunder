# just reuse the base requirements
-r base.txt

# all but the test extras, which pull in jupyter-ai and upgrade apt-owned packages in the GPU image
ipython[black,doc,kernel,matplotlib,nbconvert,nbformat,notebook,parallel,qtconsole] ~=8.37.0
numpy
liger-kernel == 0.4.0
cuda-python >=12.6.1, <14.0.0
litgpt == 0.5.1
