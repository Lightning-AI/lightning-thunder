recursive-exclude __pycache__  *.py[cod] *.orig

# Include the README and CHANGELOG
include *.md
recursive-include pl_sandbox *.md

# Include the license file
include LICENSE

# Exclude build configs, but keep pyproject.toml: the wheel is built from the
# sdist, and without it setuptools loses all [project] metadata incl. the name.
exclude *.toml
include pyproject.toml
exclude *.svg
exclude *.yml
exclude *.yaml

# exclude tests from package
recursive-exclude thunder/tests *
recursive-exclude site *
exclude thunder/tests

# Exclude the documentation files
recursive-exclude docs *
exclude docs

# Include the Requirements
recursive-include requirements *.txt

# Exclude Makefile
exclude Makefile

prune .git
prune .github
prune notebook*
prune temp*
prune test*
prune benchmark*
prune examples*
